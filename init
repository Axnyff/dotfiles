git config --global user.email "axnyff@gmail.com"
git config --global user.email "Axel Nyffenegger"
cd ~

ln -s .vim/.tmux.conf .tmux.conf
ln -s .vim/.vimrc .vimrc
ln -s .vim/.gitconfig .gitconfig

# vim
yes | sudo apt-get install libx11-dev libxtst-dev libxt-dev libsm-dev libxpm-dev sudo apt install libncurses5-dev

# tmux
sudo apt-get install tmux

# nvm
wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash
